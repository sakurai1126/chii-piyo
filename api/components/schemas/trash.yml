# ゴミ箱
TrashItemResponseDto:
  type: object
  description: ゴミ箱アイテム情報
  required:
    - id
    - media
    - expiresAt
    - createdAt
  properties:
    id:
      type: integer
      format: int64
      description: ゴミ箱ID
      example: 1
    media:
      $ref: "./media.yml#/MediaResponseDto"
    expiresAt:
      type: string
      format: date-time
      description: 完全削除予定日時
      example: "2026-02-01T00:00:00Z"
    createdAt:
      type: string
      format: date-time
      description: 作成日時
      example: "2026-01-01T00:00:00Z"

TrashItemListResponseDto:
  type: object
  description: ゴミ箱一覧レスポンス
  required:
    - items
    - earliest
    - totalCount
    - hasNext
  properties:
    items:
      type: array
      items:
        $ref: "#/TrashItemResponseDto"
    earliest:
      type: [integer, null]
      format: int64
      description: 最も期限が近いアイテムの完全削除までの残り日数
      example: 3
    totalCount:
      type: integer
      format: int64
      description: 総件数
      example: 5
    hasNext:
      type: boolean
      description: 次のページが存在するかどうか
      example: true
